nop Macro menu: holding MONI shows the icon-tile grid of quick actions; a number
nop key acts on the current channel while held; releasing MONI closes it. The
nop overlay uses the shared top bar and is otherwise opaque (no S-meter), so it
nop is deterministic on its own. Locking the keypad (key 9) shows a lock glyph in
nop the top bar of every screen, including the VFO after the menu closes.
rssi 0
sleep 1000
keyhold MONI
sleep 500
screenshot macro_open.bmp
sleep 300
nop Key 6 cycles power one step (1.0W -> 2.5W) while MONI is held.
key 6
sleep 400
screenshot macro_power.bmp
sleep 300
nop Key 9 locks the keypad; on release the VFO shows the lock glyph in its top bar.
key 9
sleep 400
keyrelease
sleep 400
screenshot macro_vfo_locked.bmp
sleep 300
quit
