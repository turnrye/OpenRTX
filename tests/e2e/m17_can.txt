nop VFO+M17 CAN detail shows only when the CAN actually gates traffic: nothing
nop while receiving with the CAN check off (promiscuous, the default — the CAN
nop filters nothing, so it is hidden), and "CAN 0" while transmitting (you key
nop up on your own CAN). Switch VFO to M17 via the macro overlay, then toggle
nop PTT.
rssi 0
sleep 1000
keyhold MONI
sleep 300
key 5
sleep 300
keyrelease MONI
sleep 500
screenshot m17_can_rx.bmp
sleep 300
nop PTT: the CAN detail appears -- the CAN you key up on.
ptt
sleep 500
screenshot m17_can_tx.bmp
sleep 300
nop Release: the CAN detail hides again (promiscuous RX, nothing gated).
ptt
sleep 500
screenshot m17_can_rx2.bmp
sleep 300
quit
